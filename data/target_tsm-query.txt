SELECT
    pl_name, pl_rade, pl_masse, pl_orbsmax,
    st_rad, st_mass, st_teff, st_spectype,
    sy_jmag, sy_vmag, sy_dist,
    disc_year
from ps WHERE default_flag = 1
AND pl_rade <= 4.0
AND sy_jmag >= 8.0